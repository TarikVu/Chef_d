// This class represents a user (Beta)
class User {
  // A way to identify this post
  int id;
  // The post's Title.
  String username;
  // The image of the post
  String role;
  // The caption of the post
  DateTime timestamp;

  User(this.id, this.username, this.role, this.timestamp);
}
